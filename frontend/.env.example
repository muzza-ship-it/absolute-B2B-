# Base URL the frontend uses to reach the backend API.
# In production this should point at the deployed backend, e.g.
# https://absoluteb2b.com/api  (see HOSTINGRAJA_DEPLOYMENT_PLAN.md for the
# reverse-proxy setup that makes /api resolve to the backend process).
VITE_API_URL=http://localhost:5000/api
